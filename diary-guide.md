# ✦ 나의 다이어리 2026

> 하나의 HTML 파일로 작동하는 개인 다이어리 웹앱.
> **안드로이드 홈화면**에 앱처럼 설치해서 사용할 수 있습니다.

---

## 📁 파일 구성

```
diary-mobile.html   ← 메인 앱
manifest.json       ← PWA 앱 정보 (홈화면 설치용)
sw.js               ← 서비스워커 (오프라인 지원)
icon.svg            ← 앱 아이콘
icon-192.png        ← 앱 아이콘 (Android, 192×192)
icon-512.png        ← 앱 아이콘 (Android, 512×512)
```

> 모든 파일은 **같은 폴더** 안에 있어야 합니다.

---

## 📱 안드로이드 홈화면에 설치하는 법

> 앱처럼 전체화면으로 실행되고, 홈화면에 아이콘이 생깁니다.

### 방법 1 — GitHub Pages (가장 완벽한 방법, 무료)

1. [github.com](https://github.com) 회원가입 / 로그인
2. 새 저장소(Repository) 생성 → **Public** 설정
3. 위 파일들을 모두 업로드
4. `Settings` → `Pages` → `Branch: main` 선택 → Save
5. 잠시 후 `https://사용자명.github.io/저장소명/diary-mobile.html` 주소 생성
6. 안드로이드 Chrome에서 해당 주소 접속
7. Chrome 우상단 `⋮` 메뉴 → **"홈 화면에 추가"**
8. 완료! 홈화면에서 앱처럼 실행됩니다 ✦

### 방법 2 — Chrome 바로가기 추가 (서버 없이 간단하게)

> 완전한 앱 모드는 아니지만 바로가기로 빠르게 열 수 있습니다.

1. 파일들을 Google Drive 또는 내 스마트폰에 저장
2. 안드로이드 Chrome에서 `diary-mobile.html` 파일 열기
3. Chrome 우상단 `⋮` 메뉴 → **"홈 화면에 추가"**
4. 홈화면에 바로가기 아이콘 생성됨

### 방법 3 — 로컬 서버 앱 사용

> 스마트폰에 서버 앱을 설치해 로컬에서 완전한 PWA로 실행

1. Play Store에서 **"Simple HTTP Server"** 앱 설치
2. 다이어리 파일들이 있는 폴더를 루트 폴더로 지정
3. 서버 시작 → `http://localhost:포트/diary-mobile.html` 접속
4. Chrome에서 홈화면에 추가

---

## 💡 홈화면 실행 시 동작 방식

| 항목 | 내용 |
|---|---|
| 첫 화면 | Monthly (월간 달력) |
| 날짜 탭 | 탭 한 번 → Daily 뷰로 바로 이동 |
| 실행 모드 | 전체화면 (주소창 없음) |
| 오프라인 | 한 번 접속 후 인터넷 없이도 사용 가능 |
| 데이터 저장 | 기기 내 브라우저 localStorage (자동 저장) |

---

## 🚀 PC에서 사용하는 법

1. `diary-mobile.html` 파일을 **더블클릭** → 브라우저에서 열림
2. 바로 사용 가능

---

## 📌 탭별 기능 안내

### 1. Monthly (월간 달력)

| 기능 | 설명 |
|---|---|
| 월 이동 | `‹` `›` 버튼으로 월 이동 |
| 날짜 메모 | 각 날짜 칸에 텍스트 메모 입력 |
| 데일리 이동 | 날짜 칸 **탭/클릭** → Daily 뷰로 이동 |
| 데일리룩 뱃지 | 룩 기록이 있는 날짜에 베이지 점(●) 표시 |
| 주간 평가 | 주차별 별점(★×5) + 주간 소감 입력 |
| 검색 | 기온 또는 키워드로 전체 데이터 검색 |

#### 🔍 검색 기능
- **기온 검색**: 숫자 입력 (예: `22`, `-5`)
  → 해당 기온이 최저~최고 범위에 포함되는 데일리룩 검색
  → 영하(마이너스) 기온도 지원 (예: `-3`)
- **키워드 검색**: 단어 입력 (예: `어린이집 특별활동비`)
  → 달력메모 / 루틴 / 타임라인 / 두잇리스트 / 감사일기 / 육아일기 / 데일리룩 메모 전체 검색
  → 결과 클릭 시 해당 날짜 Daily로 바로 이동

---

### 2. Daily (일간 다이어리)

날짜 클릭 → 날짜 선택기 / `‹` `›` 버튼으로 하루씩 이동

| 섹션 | 기능 |
|---|---|
| ☀️ Routine | 체크박스 + 루틴 항목 (완료 시 취소선) / 루틴 추가 버튼 |
| 🕐 Time Line | 06:00~22:00 시간대별 일정 입력 |
| ✅ Do It List | 우선순위 점(h/m/l) + 체크박스 + 할 일 입력 / **완료 체크 시 취소선** |
| 💡 Idea | 자유 메모 (형광펜 적용 가능) |
| 🌸 감사일기 | 오늘 감사한 일 3가지 (형광펜 적용 가능) |
| 👶 육아일기 | 아이와의 소중한 순간 기록 (형광펜 적용 가능) |
| 💰 지출메모 | 카테고리 + 항목 + 금액 / 자동 합계 계산 |

#### Do It List 우선순위
- 점 클릭할 때마다 순환: 🔴 높음(h) → 🟡 보통(m) → 🔵 낮음(l)

---

### 3. Daily Look (데일리룩)

옷 기록과 날씨 정보를 함께 저장해 **기온별 코디 검색**이 가능한 페이지

| 기능 | 설명 |
|---|---|
| 룩 추가 | `+` FAB 버튼 |
| 날짜 선택 | 날짜 입력 시 날씨 자동 불러오기 |
| 날씨 자동불러오기 | `🌤 날씨 불러오기` 버튼 (오늘 기준 ±14일 지원) |
| 사진 업로드 | 사진 파일 선택 |
| 메모 | 옷 설명, 느낌 등 자유 메모 |
| 삭제 | 카드의 ✕ 버튼 |

#### 🌤 날씨 자동불러오기
- **Open-Meteo API** 사용 (무료, API 키 불필요)
- 브라우저에서 **위치 권한 허용** 필요
- 모달을 열면 오늘 날씨 자동 적용
- 날짜 변경 시 해당 날짜 날씨 자동 업데이트
- 오늘 기준 ±14일 범위 이내만 자동 지원 (그 외는 직접 입력)

---

### 4. Script (스크립트 노트)

신문, 밀리의서재, 블로그 등에서 읽은 문구를 분야별로 정리하는 페이지

#### 분야 폴더
- 기본 폴더: 📰 경제신문 공부 / 📚 책 / ✨ 영감
- 하단 입력창으로 **새 폴더 자유롭게 추가** (Enter 또는 + 버튼)
- 폴더 **우클릭** → 폴더 삭제
- 폴더마다 스크립트 개수 표시

#### 스크립트 카드
| 항목 | 설명 |
|---|---|
| 출처 | 신문 / 밀리의서재 / 블로그 / 유튜브 / 팟캐스트 / 기타 선택 |
| 날짜 | 오늘 날짜 자동 입력 |
| 제목 | 자유 입력 |
| QUOTE | 인용문 영역 — 직접 입력 또는 `📋 가져오기` |
| MEMO | 내 생각 메모 — 형광펜 적용 가능 |

#### 문구 가져오기
- **직접 붙여넣기**: 신문/책 등에서 복사한 텍스트 붙여넣기
- **URL 입력**: 웹페이지 주소를 입력하면 텍스트 자동 추출
  (일부 사이트는 보안 정책으로 제한될 수 있음)

---

## 🎨 공통 도구

### 글씨 색
- 상단 툴바에 **18가지 프리셋 색상** 점(●) 클릭
- 무지개 버튼 → **직접 색상 선택** (커스텀 컬러피커)
- 입력창에 포커스 시 선택한 색으로 자동 적용

### 형광펜 하이라이트
- **적용 가능 영역**: Routine / TimeLine / Do It List / Idea / 감사일기 / 육아일기 / Script 메모

| 방법 | 설명 |
|---|---|
| 드래그 후 팝업 | 텍스트 드래그 → 색상 팝업 자동 등장 → 색 클릭 |
| 툴바 버튼 | 형광펜 색 선택 후 `형광펜 적용` 버튼 클릭 |
| 단축키 | 텍스트 드래그 후 `Alt + H` |
| 제거 | ✕ 선택 후 적용 → 형광펜 제거 |

---

## ⌨️ 단축키

| 단축키 | 동작 |
|---|---|
| `Alt + H` | 선택한 텍스트에 형광펜 적용 |
| `Esc` | 형광펜 팝업 닫기 |
| `Enter` | 새 폴더 추가 (Script 탭 폴더 입력창) |
| 날짜 탭/클릭 | Monthly → Daily 이동 |

---

## 🛠 기술 정보

| 항목 | 내용 |
|---|---|
| 파일 형식 | HTML + PWA 파일 세트 |
| 프레임워크 | Vanilla JS (라이브러리 없음) |
| 데이터 저장 | localStorage (새로고침 후에도 유지) |
| PWA | manifest.json + Service Worker |
| 날씨 API | Open-Meteo (무료, 키 불필요) |
| 위치 API | Nominatim (OpenStreetMap, 무료) |
| URL 가져오기 | allorigins.win 프록시 경유 |
| 폰트 | Nanum Myeongjo, Noto Sans KR (Google Fonts) |

---

## 📝 향후 개선 아이디어

- [ ] 데이터 내보내기 / 가져오기 (JSON 백업)
- [ ] 다크모드 지원
- [ ] Script 태그 기능
- [ ] 달력 이미지 내보내기
- [x] localStorage 연동 (완료)
- [x] 모바일 최적화 / PWA 설치 (완료)

---

> 만든 날: 2026년 3월
> 제작: Claude (Anthropic) with 바이브 코딩 ✦
